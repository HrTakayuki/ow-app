import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  fonts: {
    heading: `"M PLUS Rounded 1c", "Noto Sans JP", "Arial Rounded MT Bold", Arial, sans-serif`,
    body: `"M PLUS Rounded 1c", "Noto Sans JP", "Arial Rounded MT Bold", Arial, sans-serif`,
  },
});

export default theme;
